import { BusinessService.Model } from './business-service.model';

describe('BusinessService.Model', () => {
  it('should create an instance', () => {
    expect(new BusinessService.Model()).toBeTruthy();
  });
});
